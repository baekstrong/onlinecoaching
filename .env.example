# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# 코치(관리자) 계정으로 지정할 이메일 — 최초 로그인(확인된 이메일) 시에만 coach 역할 부여
COACH_EMAIL=

# 앱의 정식 URL — OAuth 리다이렉트 기준(Host 헤더 스푸핑 방지). 로컬은 http://localhost:3000
APP_URL=

# (추후 단계) Cloudflare R2
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
